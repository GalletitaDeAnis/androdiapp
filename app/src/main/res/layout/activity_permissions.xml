<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Solicitar Permiso o Comisión"
        android:textSize="20sp"
        android:gravity="center"
        android:padding="8dp" />

    <!-- Spinner para seleccionar tipo de permiso -->
    <Spinner
        android:id="@+id/permissionTypeSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/permission_types"
        android:prompt="@string/seleccionar_tipo_permiso" />

    <!-- EditText para el motivo y detalles del permiso -->
    <EditText
        android:id="@+id/permissionReasonEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Fecha deseada, motivo y duración en días"
        android:inputType="textMultiLine"
        android:minLines="3" />

    <!-- Botón para solicitar el permiso -->
    <Button
        android:id="@+id/requestPermissionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Solicitar Permiso"
        android:layout_marginTop="16dp"
        android:layout_gravity="center" />
</LinearLayout>
